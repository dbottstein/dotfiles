source $dotdir/.dotfiles/zplug/init.sh

for plugin in $plugins; do
	zplug "plugins/$plugin", from:oh-my-zsh
	test -d ~/.dotfiles/zsh/plugins/$plugin && zplug "$zdotdir/.dotfiles/zsh/plugins/$plugin", from:local
done

zplug check || zplug install
zplug update

